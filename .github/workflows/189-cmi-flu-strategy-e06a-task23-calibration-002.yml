name: Retired CMI-Flu E06a Task2.3 calibration CPU 002

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E06a 002 replay
        shell: bash
        run: |
          echo "RETIRED: E06a 002 was written as version 1, exact identity was confirmed, scientific computation reached validation, and the bridge validator rejected a legitimate non-promoted candidate. Replay is forbidden; use the fresh audited successor request."
          exit 1
