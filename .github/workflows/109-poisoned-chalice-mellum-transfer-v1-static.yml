name: Poisoned Chalice Mellum transfer v1 static validation

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/109-poisoned-chalice-mellum-transfer-v1-static.yml
  pull_request:
    paths:
      - .github/workflows/109-poisoned-chalice-mellum-transfer-v1-static.yml

permissions: {}

jobs:
  static:
    name: Probe workflow startup without credentials
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Startup probe
        shell: bash
        run: echo "MELLUM_STATIC_STARTUP PASS"
