name: Poisoned Chalice Stage1 request005 status and output verification

on:
  push:
    branches:
      - main
    paths:
      - requests/poisoned-chalice-stage1-request005-status-v1.json

permissions: {}

concurrency:
  group: kaggle-readonly-poisoned-chalice-stage1-request005
  cancel-in-progress: false

env:
  EXPECTED_REPOSITORY: renta0426/kaggle-actions-bridge
  EXPECTED_REPOSITORY_ID: "1354356687"
  EXPECTED_ACTOR: renta0426
  EXPECTED_ACTOR_ID: "71638068"
  REQUEST_PATH: requests/poisoned-chalice-stage1-request005-status-v1.json
  LOCK_RELATIVE_PATH: requirements/kaggle-2.2.4.lock
  TARGET_COMPETITION: poisoned-chalice-icse27
  TARGET_KERNEL: renta0426/stage1-raw-fim-submission-v1
  EXPECTED_KERNEL_VERSION: "2"

jobs:
  verify:
    name: Verify request005 version 2 outputs without side effects
    if: >-
      github.repository == 'renta0426/kaggle-actions-bridge' &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      github.actor == 'renta0426' &&
      github.actor_id == '71638068'
    environment: kaggle-readonry
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Enforce protected read-only boundary
        shell: bash
        env:
          EVENT_REPOSITORY: ${{ github.repository }}
          EVENT_REPOSITORY_ID: ${{ github.event.repository.id }}
          EVENT_ACTOR: ${{ github.actor }}
          EVENT_ACTOR_ID: ${{ github.actor_id }}
          EVENT_SENDER_ID: ${{ github.event.sender.id }}
          EVENT_NAME: ${{ github.event_name }}
          EVENT_REF: ${{ github.ref }}
          EVENT_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
          WORKFLOW_REF: ${{ github.workflow_ref }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "${EVENT_REPOSITORY}" == "${EXPECTED_REPOSITORY}" ]]
          [[ "${EVENT_REPOSITORY_ID}" == "${EXPECTED_REPOSITORY_ID}" ]]
          [[ "${EVENT_ACTOR}" == "${EXPECTED_ACTOR}" ]]
          [[ "${EVENT_ACTOR_ID}" == "${EXPECTED_ACTOR_ID}" ]]
          [[ "${EVENT_SENDER_ID}" == "${EXPECTED_ACTOR_ID}" ]]
          [[ "${EVENT_NAME}" == "push" ]]
          [[ "${EVENT_REF}" == "refs/heads/main" ]]
          [[ "${EVENT_SHA}" == "${WORKFLOW_SHA}" ]]
          [[ "${WORKFLOW_REF}" == "renta0426/kaggle-actions-bridge/.github/workflows/81-poisoned-chalice-stage1-request005-status.yml@refs/heads/main" ]]
          [[ "${RUNNER_ENVIRONMENT}" == "github-hosted" ]]
          echo "POISONED_CHALICE_REQUEST005_STATUS_BOUNDARY PASS"

      - name: Validate exact read-only request and prepare locked Kaggle CLI
        shell: bash
        env:
          EVENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"
          echo "WORKDIR=${workdir}" >> "${GITHUB_ENV}"
          request_url="https://raw.githubusercontent.com/${EXPECTED_REPOSITORY}/${EVENT_SHA}/${REQUEST_PATH}"
          lock_url="https://raw.githubusercontent.com/${EXPECTED_REPOSITORY}/${EVENT_SHA}/${LOCK_RELATIVE_PATH}"
          python3 - "${request_url}" "${lock_url}" "${workdir}/kaggle.lock" <<'PY'
          from __future__ import annotations
          import json
          import re
          import ssl
          import sys
          import urllib.request
          from urllib.parse import urlparse

          request_url, lock_url, lock_output = sys.argv[1:]

          class RejectRedirects(urllib.request.HTTPRedirectHandler):
              def redirect_request(self, req, fp, code, msg, headers, newurl):
                  return None

          opener = urllib.request.build_opener(
              urllib.request.ProxyHandler({}),
              urllib.request.HTTPSHandler(context=ssl.create_default_context()),
              RejectRedirects(),
          )

          def read(url: str, limit: int) -> bytes:
              parsed = urlparse(url)
              if parsed.scheme != "https" or parsed.hostname != "raw.githubusercontent.com":
                  raise SystemExit("unexpected raw source origin")
              with opener.open(url, timeout=30) as response:
                  data = response.read(limit + 1)
              if len(data) > limit:
                  raise SystemExit("input exceeds fixed byte budget")
              return data

          request = json.loads(read(request_url, 16384))
          expected = {
              "schema_version": 1,
              "request_id": "20260903-poisoned-chalice-stage1-request005-status-001",
              "parent_request_id": "20260902-poisoned-chalice-stage1-raw-fim-005",
              "competition": "poisoned-chalice-icse27",
              "operation": "kernel_status_output",
              "target": "renta0426/stage1-raw-fim-submission-v1",
              "expected_kernel_version": 2,
              "api_budget": {"max_calls": 8, "max_pages": 1},
              "side_effects": [],
          }
          if request != expected:
              raise SystemExit("request005 status request differs from exact allowlist")

          text = read(lock_url, 65536).decode("utf-8")
          pattern = re.compile(r"^[a-z0-9][a-z0-9._-]*==[A-Za-z0-9][A-Za-z0-9._+-]* --hash=sha256:[0-9a-f]{64}$")
          lines = [line.strip() for line in text.splitlines() if line.strip() and not line.lstrip().startswith("#")]
          if len(lines) != 33 or not all(pattern.fullmatch(line) for line in lines):
              raise SystemExit("Kaggle lock validation failed")
          if not any(line.startswith("kaggle==2.2.4 ") for line in lines):
              raise SystemExit("Kaggle 2.2.4 pin missing")
          open(lock_output, "w", encoding="utf-8").write(text)
          print("REQUEST005_STATUS_REQUEST PASS side_effects=none")
          PY
          python3 -m venv "${workdir}/venv"
          "${workdir}/venv/bin/python" -m pip install \
            --disable-pip-version-check --no-input --no-deps \
            --only-binary=:all: --require-hashes \
            -r "${workdir}/kaggle.lock" >/dev/null
          "${workdir}/venv/bin/python" -m pip check >/dev/null
          "${workdir}/venv/bin/kaggle" --version | grep -F "2.2.4" >/dev/null

      - name: Verify live policy, private version, and completed state
        shell: bash
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          [[ -n "${KAGGLE_API_TOKEN}" && "${KAGGLE_API_TOKEN}" == KGAT_* ]]
          "${WORKDIR}/venv/bin/python" - <<'PY'
          from __future__ import annotations
          import os
          import re
          from kaggle.api.kaggle_api_extended import KaggleApi
          from kagglesdk.kernels.types.kernels_api_service import ApiGetKernelRequest

          api = KaggleApi()
          api.authenticate()
          pages = api.competition_list_pages(os.environ["TARGET_COMPETITION"]) or []
          content = {}
          for page in pages:
              data = page.to_dict() if hasattr(page, "to_dict") else dict(page)
              name = str(data.get("name") or "").strip().lower()
              if name:
                  content[name] = str(data.get("content") or "")
          if "rules" not in content or "evaluation" not in content:
              raise SystemExit("live Competition policy pages unavailable")
          evaluation = re.sub(
              r"\s+", " ", re.sub(r"<[^>]+>", " ", content["evaluation"].replace(r"\%", "%"))
          ).casefold()
          if not all(term in evaluation for term in ("auc", "novelty", "1%", "false-positive")):
              raise SystemExit("live evaluation contract mismatch")

          with api.build_kaggle_client() as client:
              request = ApiGetKernelRequest()
              request.user_name = "renta0426"
              request.kernel_slug = "stage1-raw-fim-submission-v1"
              latest = client.kernels.kernels_api_client.get_kernel(request)
          version = int(latest.metadata.current_version_number)
          private = bool(latest.metadata.is_private)
          if version != int(os.environ["EXPECTED_KERNEL_VERSION"]) or not private:
              raise SystemExit(f"unexpected target version/privacy: version={version} private={private}")

          status = api.kernels_status(os.environ["TARGET_KERNEL"])
          value = str(getattr(status, "status", status)).upper()
          if "COMPLETE" not in value:
              if any(token in value for token in ("RUNNING", "QUEUED", "PENDING")):
                  raise SystemExit("request005 target is still active; no output was downloaded")
              raise SystemExit(f"request005 target did not complete successfully: {value}")
          print(f"REQUEST005_KERNEL_COMPLETE version={version} private=true")
          PY

      - name: Download only final manifest and submission, then verify
        shell: bash
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          output_dir="${WORKDIR}/output"
          mkdir -p "${output_dir}"
          set +e
          "${WORKDIR}/venv/bin/kaggle" kernels output "${TARGET_KERNEL}" \
            -p "${output_dir}" -q -o --page-size 50 \
            --file-pattern '^(run_manifest\.json|submission\.csv)$' \
            >"${WORKDIR}/output.stdout" 2>"${WORKDIR}/output.stderr"
          rc=$?
          set -e
          rm -f "${WORKDIR}/output.stdout" "${WORKDIR}/output.stderr"
          if [[ "${rc}" -ne 0 ]]; then
            echo "ERROR: bounded Kaggle output download failed; response suppressed"
            exit 1
          fi

          "${WORKDIR}/venv/bin/python" - "${output_dir}" <<'PY'
          from __future__ import annotations
          import csv
          import hashlib
          import json
          import math
          import pathlib
          import sys

          root = pathlib.Path(sys.argv[1])
          files = [path for path in root.rglob("*") if path.is_file()]
          if {path.name for path in files} != {"run_manifest.json", "submission.csv"}:
              raise SystemExit("output file allowlist mismatch")
          manifest_path = next(path for path in files if path.name == "run_manifest.json")
          submission_path = next(path for path in files if path.name == "submission.csv")
          manifest = json.loads(manifest_path.read_text(encoding="utf-8"))

          expected = {
              "status": "complete",
              "method": "raw_plus_fim",
              "core_commit": "bcf6d44db86fdda4ac9dd07b2d0172de471ab67a",
              "source_commit": "28c34307c71db733dab2744ec9ec46549e54d424",
              "model_id": "bigcode/starcoder2-3b",
              "model_revision": "733247c55e3f73af49ce8e9c7949bf14af205928",
              "dataset_id": "Poisoned-Chalice/ICSE-2027-public",
              "dataset_revision": "2ed5468723efa5457a3665782c6979ea4dbac7c2",
              "train_rows": 10000,
              "validation_rows": 5000,
              "submission_rows": 5000,
              "hidden_validation_labels_used": False,
              "public_leaderboard_tuning_used": False,
              "validation_labels_used_for_fit_or_feature_selection": False,
              "assembled_from_private_kaggle_sources": True,
          }
          for key, value in expected.items():
              if manifest.get(key) != value:
                  raise SystemExit(f"kernel manifest mismatch for {key}")

          counts = manifest.get("feature_counts") or {}
          if counts != {
              "base": 113,
              "structure_raw": 50,
              "fim": 11,
              "total": 174,
              "cdf_added_at_fit": 3,
          }:
              raise SystemExit(f"feature-family contract mismatch: {counts}")
          oof = manifest.get("reproduced_oof") or {}
          auc = float(oof.get("auc", float("nan")))
          tpr = float(oof.get("tpr_at_0.01_fpr", float("nan")))
          if not math.isfinite(auc) or abs(auc - 0.664524) > 0.002:
              raise SystemExit(f"frozen OOF AUC reproduction mismatch: {auc}")
          if not math.isfinite(tpr):
              raise SystemExit("frozen OOF TPR diagnostic missing")

          digest = hashlib.sha256(submission_path.read_bytes()).hexdigest()
          if digest != manifest.get("submission_sha256"):
              raise SystemExit("submission SHA-256 differs from kernel manifest")
          with submission_path.open(newline="", encoding="utf-8") as handle:
              reader = csv.DictReader(handle)
              if reader.fieldnames != ["sample_id", "membership_score"]:
                  raise SystemExit("submission header mismatch")
              rows = list(reader)
          if len(rows) != 5000 or len({row["sample_id"] for row in rows}) != 5000:
              raise SystemExit("submission sample coverage mismatch")
          scores = [float(row["membership_score"]) for row in rows]
          if not all(math.isfinite(value) for value in scores):
              raise SystemExit("submission contains non-finite score")

          visible = manifest.get("visible_validation_diagnostic_only") or {}
          visible_auc = float(visible.get("auc", float("nan")))
          print(
              "POISONED_CHALICE_REQUEST005_OUTPUT_VERIFIED "
              f"submission_sha256={digest} rows=5000 oof_auc={auc:.6f} "
              f"oof_tpr_1pct={tpr:.4f} visible_auc={visible_auc:.6f}"
          )
          PY

      - name: Cleanup runner-local output
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${WORKDIR-}" && -d "${WORKDIR}" ]]; then
            rm -rf "${WORKDIR}"
          fi
          echo "REQUEST005_STATUS_TEMPORARY_MATERIAL_REMOVED"
