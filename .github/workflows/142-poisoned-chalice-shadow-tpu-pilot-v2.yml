name: Poisoned Chalice self-contained shadow TPU pilot v2

on:
  push:
    branches:
      - main
    paths:
      - requests/poisoned-chalice-shadow-tpu-pilot-v1.json
      - scripts/poisoned_chalice_shadow_tpu_pilot_v2.py
      - materialized/poisoned-chalice-shadow-tpu-pilot-v1/**
      - .github/workflows/142-poisoned-chalice-shadow-tpu-pilot-v2.yml
  pull_request:
    paths:
      - requests/poisoned-chalice-shadow-tpu-pilot-v1.json
      - scripts/poisoned_chalice_shadow_tpu_pilot_v2.py
      - materialized/poisoned-chalice-shadow-tpu-pilot-v1/**
      - .github/workflows/142-poisoned-chalice-shadow-tpu-pilot-v2.yml

permissions: {}

env:
  EXPECTED_REPOSITORY: renta0426/kaggle-actions-bridge
  EXPECTED_REPOSITORY_ID: "1354356687"
  EXPECTED_ACTOR: renta0426
  EXPECTED_ACTOR_ID: "71638068"
  REQUEST_PATH: requests/poisoned-chalice-shadow-tpu-pilot-v1.json
  LAUNCHER_PATH: scripts/poisoned_chalice_shadow_tpu_pilot_v2.py
  LOCK_PATH: requirements/kaggle-2.2.4.lock

jobs:
  validate:
    name: Validate self-contained shadow TPU pilot v2
    if: >-
      github.repository == 'renta0426/kaggle-actions-bridge' &&
      github.actor == 'renta0426' &&
      github.actor_id == '71638068'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Enforce secret-free boundary
        shell: bash
        env:
          EVENT_REPOSITORY: ${{ github.repository }}
          EVENT_REPOSITORY_ID: ${{ github.event.repository.id }}
          EVENT_ACTOR: ${{ github.actor }}
          EVENT_ACTOR_ID: ${{ github.actor_id }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "${EVENT_REPOSITORY}" == "${EXPECTED_REPOSITORY}" ]]
          [[ "${EVENT_REPOSITORY_ID}" == "${EXPECTED_REPOSITORY_ID}" ]]
          [[ "${EVENT_ACTOR}" == "${EXPECTED_ACTOR}" ]]
          [[ "${EVENT_ACTOR_ID}" == "${EXPECTED_ACTOR_ID}" ]]
          [[ "${RUNNER_ENVIRONMENT}" == "github-hosted" ]]
          for name in KAGGLE_API_TOKEN RESEARCH_REPO_READ_TOKEN KAGGLE_USERNAME KAGGLE_KEY GH_TOKEN GITHUB_PAT SSH_PRIVATE_KEY AWS_ACCESS_KEY_ID AZURE_CLIENT_SECRET; do
            [[ -z "${!name-}" ]] || { echo "unexpected credential variable: ${name}"; exit 1; }
          done
          echo 'SHADOW_TPU_V2_SECRET_FREE PASS private_repo_access=0'

      - name: Reconstruct only from approved bridge commit
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SHA: ${{ github.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          source_sha="${EVENT_SHA}"
          if [[ "${EVENT_NAME}" == "pull_request" ]]; then source_sha="${PR_HEAD_SHA}"; fi
          [[ "${source_sha}" =~ ^[0-9a-f]{40}$ ]]
          workdir="$(mktemp -d)"
          trap 'rm -rf "${workdir}"' EXIT
          python3 - "${source_sha}" "${workdir}" <<'PY'
          import hashlib, json, pathlib, ssl, sys, urllib.request
          from urllib.parse import urlparse
          sha, root_value = sys.argv[1:]
          root = pathlib.Path(root_value)
          paths = {
              "requests/poisoned-chalice-shadow-tpu-pilot-v1.json": 32768,
              "scripts/poisoned_chalice_shadow_tpu_pilot_v2.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/poisoned_chalice/shadow_protocol.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/poisoned_chalice/shadow_training.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/shadow_tpu_pilot_v1.json": 32768,
          }
          context = ssl.create_default_context()
          for path, maximum in paths.items():
              url = f"https://raw.githubusercontent.com/renta0426/kaggle-actions-bridge/{sha}/{path}"
              parsed = urlparse(url)
              if parsed.scheme != "https" or parsed.hostname != "raw.githubusercontent.com":
                  raise SystemExit("unexpected bridge origin")
              req = urllib.request.Request(url, headers={"User-Agent": "kaggle-actions-bridge/1"})
              with urllib.request.urlopen(req, timeout=30, context=context) as response:
                  data = response.read(maximum + 1)
              if len(data) > maximum:
                  raise SystemExit(f"bridge file exceeds byte budget: {path}")
              dst = root / path
              dst.parent.mkdir(parents=True, exist_ok=True)
              dst.write_bytes(data)
          request = json.loads((root / "requests/poisoned-chalice-shadow-tpu-pilot-v1.json").read_text())
          launcher = root / request["launcher_path"]
          data = launcher.read_bytes()
          blob = hashlib.sha1(b"blob " + str(len(data)).encode() + b"\0" + data).hexdigest()
          if blob != request["launcher_blob_sha"]:
              raise SystemExit("launcher blob mismatch")
          compile(data, request["launcher_path"], "exec")
          for item in request["materialized_files"]:
              data = (root / item["bridge_path"]).read_bytes()
              blob = hashlib.sha1(b"blob " + str(len(data)).encode() + b"\0" + data).hexdigest()
              if blob != item["git_blob_sha"]:
                  raise SystemExit(f"snapshot blob mismatch: {item['bridge_path']}")
          if request["clean_room"].get("protected_job_private_repository_access") is not False:
              raise SystemExit("private-repo access guard changed")
          print('SHADOW_TPU_V2_SNAPSHOT PASS snapshots=3 private_repo_access=0')
          PY
          python3 "${workdir}/${LAUNCHER_PATH}" \
            --static \
            --request "${workdir}/${REQUEST_PATH}" \
            --launcher "${workdir}/${LAUNCHER_PATH}"
          python3 "${workdir}/${LAUNCHER_PATH}" \
            --materialize \
            --request "${workdir}/${REQUEST_PATH}" \
            --launcher "${workdir}/${LAUNCHER_PATH}" \
            --snapshot-root "${workdir}" \
            --kernel-dir "${workdir}/kernel"
          python3 - "${workdir}/kernel" <<'PY'
          import json, pathlib, sys
          root = pathlib.Path(sys.argv[1])
          if sorted(p.name for p in root.iterdir()) != ["kernel-metadata.json", "shadow-tpu-pilot-v1.ipynb"]:
              raise SystemExit("kernel bundle allowlist changed")
          notebook = json.loads((root / "shadow-tpu-pilot-v1.ipynb").read_text())
          compile("".join(notebook["cells"][1]["source"]), "shadow-tpu-pilot-v1.ipynb", "exec")
          print('SHADOW_TPU_V2_NOTEBOOK_COMPILE PASS')
          PY

  launch:
    name: Launch exactly one self-contained shadow TPU pilot
    needs: validate
    if: >-
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      github.repository == 'renta0426/kaggle-actions-bridge' &&
      github.actor == 'renta0426' &&
      github.actor_id == '71638068'
    concurrency:
      group: kaggle-resource-tpu-global
      cancel-in-progress: false
    environment: kaggle-readonry
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Enforce protected boundary
        shell: bash
        env:
          EVENT_REPOSITORY: ${{ github.repository }}
          EVENT_REPOSITORY_ID: ${{ github.event.repository.id }}
          EVENT_ACTOR: ${{ github.actor }}
          EVENT_ACTOR_ID: ${{ github.actor_id }}
          EVENT_SENDER_ID: ${{ github.event.sender.id }}
          EVENT_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
          WORKFLOW_REF: ${{ github.workflow_ref }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "${EVENT_REPOSITORY}" == "${EXPECTED_REPOSITORY}" ]]
          [[ "${EVENT_REPOSITORY_ID}" == "${EXPECTED_REPOSITORY_ID}" ]]
          [[ "${EVENT_ACTOR}" == "${EXPECTED_ACTOR}" ]]
          [[ "${EVENT_ACTOR_ID}" == "${EXPECTED_ACTOR_ID}" ]]
          [[ "${EVENT_SENDER_ID}" == "${EXPECTED_ACTOR_ID}" ]]
          [[ "${EVENT_SHA}" == "${WORKFLOW_SHA}" ]]
          [[ "${WORKFLOW_REF}" == "renta0426/kaggle-actions-bridge/.github/workflows/142-poisoned-chalice-shadow-tpu-pilot-v2.yml@refs/heads/main" ]]
          [[ "${RUNNER_ENVIRONMENT}" == "github-hosted" ]]
          [[ -z "${RESEARCH_REPO_READ_TOKEN-}" ]]
          echo 'SHADOW_TPU_V2_PROTECTED_BOUNDARY PASS private_repo_access=0'

      - name: Materialize exact bridge snapshot and isolated Kaggle client
        shell: bash
        env:
          EVENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"
          kernel_dir="${workdir}/kernel"
          echo "WORKDIR=${workdir}" >> "${GITHUB_ENV}"
          echo "KERNEL_DIR=${kernel_dir}" >> "${GITHUB_ENV}"
          python3 - "${EVENT_SHA}" "${workdir}" <<'PY'
          import hashlib, json, pathlib, re, ssl, sys, urllib.request
          from urllib.parse import urlparse
          sha, root_value = sys.argv[1:]
          root = pathlib.Path(root_value)
          paths = {
              "requests/poisoned-chalice-shadow-tpu-pilot-v1.json": 32768,
              "scripts/poisoned_chalice_shadow_tpu_pilot_v2.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/poisoned_chalice/shadow_protocol.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/poisoned_chalice/shadow_training.py": 131072,
              "materialized/poisoned-chalice-shadow-tpu-pilot-v1/shadow_tpu_pilot_v1.json": 32768,
              "requirements/kaggle-2.2.4.lock": 65536,
          }
          context = ssl.create_default_context()
          for path, maximum in paths.items():
              url = f"https://raw.githubusercontent.com/renta0426/kaggle-actions-bridge/{sha}/{path}"
              parsed = urlparse(url)
              if parsed.scheme != "https" or parsed.hostname != "raw.githubusercontent.com":
                  raise SystemExit("unexpected bridge origin")
              req = urllib.request.Request(url, headers={"User-Agent": "kaggle-actions-bridge/1"})
              with urllib.request.urlopen(req, timeout=30, context=context) as response:
                  data = response.read(maximum + 1)
              if len(data) > maximum:
                  raise SystemExit(f"bridge file exceeds byte budget: {path}")
              dst = root / path
              dst.parent.mkdir(parents=True, exist_ok=True)
              dst.write_bytes(data)
          request = json.loads((root / "requests/poisoned-chalice-shadow-tpu-pilot-v1.json").read_text())
          launcher = root / request["launcher_path"]
          data = launcher.read_bytes()
          blob = hashlib.sha1(b"blob " + str(len(data)).encode() + b"\0" + data).hexdigest()
          if blob != request["launcher_blob_sha"]:
              raise SystemExit("launcher blob mismatch")
          for item in request["materialized_files"]:
              data = (root / item["bridge_path"]).read_bytes()
              blob = hashlib.sha1(b"blob " + str(len(data)).encode() + b"\0" + data).hexdigest()
              if blob != item["git_blob_sha"]:
                  raise SystemExit(f"snapshot blob mismatch: {item['bridge_path']}")
          lock = (root / "requirements/kaggle-2.2.4.lock").read_text()
          pattern = re.compile(r"^[a-z0-9][a-z0-9._-]*==[A-Za-z0-9][A-Za-z0-9._+-]* --hash=sha256:[0-9a-f]{64}$")
          lines = [line.strip() for line in lock.splitlines() if line.strip() and not line.lstrip().startswith("#")]
          if len(lines) != 33 or not all(pattern.fullmatch(line) for line in lines):
              raise SystemExit("Kaggle CLI lock validation failed")
          PY
          python3 "${workdir}/${LAUNCHER_PATH}" --static --request "${workdir}/${REQUEST_PATH}" --launcher "${workdir}/${LAUNCHER_PATH}"
          python3 "${workdir}/${LAUNCHER_PATH}" --materialize --request "${workdir}/${REQUEST_PATH}" --launcher "${workdir}/${LAUNCHER_PATH}" --snapshot-root "${workdir}" --kernel-dir "${kernel_dir}"
          python3 -m venv "${workdir}/kaggle-venv"
          "${workdir}/kaggle-venv/bin/python" -m pip install --disable-pip-version-check --no-input --no-deps --only-binary=:all: --require-hashes -r "${workdir}/${LOCK_PATH}" >/dev/null
          "${workdir}/kaggle-venv/bin/python" -m pip check >/dev/null
          "${workdir}/kaggle-venv/bin/kaggle" --version | grep -F '2.2.4' >/dev/null
          echo 'SHADOW_TPU_V2_CLIENT PASS private_repo_access=0'

      - name: Confirm current Kaggle policy pages
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import ssl, urllib.request
          context = ssl.create_default_context()
          for url in (
              "https://www.kaggle.com/terms",
              "https://www.kaggle.com/aup",
              "https://www.kaggle.com/community-guidelines",
              "https://www.kaggle.com/docs/api",
              "https://www.kaggle.com/docs/notebooks",
          ):
              req = urllib.request.Request(url, headers={"User-Agent": "kaggle-actions-bridge/1"})
              with urllib.request.urlopen(req, timeout=30, context=context) as response:
                  if response.status != 200 or not response.read(65536):
                      raise SystemExit(f"official Kaggle page unavailable: {url}")
          print('SHADOW_TPU_V2_POLICY PASS')
          PY

      - name: Live preflight and launch once
        shell: bash
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          [[ -z "${RESEARCH_REPO_READ_TOKEN-}" ]]
          [[ -n "${KAGGLE_API_TOKEN}" && "${KAGGLE_API_TOKEN}" == KGAT_* ]]
          "${WORKDIR}/kaggle-venv/bin/python" "${WORKDIR}/${LAUNCHER_PATH}" \
            --execute \
            --request "${WORKDIR}/${REQUEST_PATH}" \
            --launcher "${WORKDIR}/${LAUNCHER_PATH}" \
            --kaggle-bin "${WORKDIR}/kaggle-venv/bin/kaggle" \
            --kernel-dir "${KERNEL_DIR}"

      - name: Cleanup runner-local material
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${WORKDIR-}" && -d "${WORKDIR}" ]]; then rm -rf "${WORKDIR}"; fi
          echo 'SHADOW_TPU_V2_CLEANUP PASS retention_days=0'
