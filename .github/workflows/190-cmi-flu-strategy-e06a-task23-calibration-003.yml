name: Retired CMI-Flu E06a Task2.3 calibration CPU 003

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E06a 003 replay
        shell: bash
        run: |
          echo "RETIRED: E06a 003 was written as version 1, exact identity was confirmed, scientific execution and validation completed, then the inherited E05 completion marker raised KeyError:'tasks' during write_outputs. Replay is forbidden; recover current version 1 read-only."
          exit 1
