name: Retired CMI-Flu E05 HAI donor strain CPU 003

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E05 003 launcher
        run: |
          echo "RETIRED: E05 003 was consumed and failed at run_e05; replay is forbidden."
          exit 1
