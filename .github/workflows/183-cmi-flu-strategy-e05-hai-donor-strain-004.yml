name: Retired CMI-Flu E05 HAI donor strain CPU 004

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired one-shot workflow
        run: |
          echo "RETIRED: E05 004 was consumed and failed; duplicate compute is refused."
          exit 1
