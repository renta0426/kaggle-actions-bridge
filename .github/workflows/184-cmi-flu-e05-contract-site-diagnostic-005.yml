name: Retired CMI-Flu E05 contract-site diagnostic 005

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired workflow execution
        run: |
          echo "RETIRED: E05 005 diagnostic already produced the required safe failure site; rerun is forbidden."
          exit 1
