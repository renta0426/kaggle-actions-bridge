name: RETIRED CMI-Flu strategy E06c D28 calibration CPU 001 prewrite-broken

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse replay of prewrite-broken E06c workflow
        shell: bash
        run: |
          echo "RETIRED: run 34314341291 failed in local argparse before Kaggle authentication, duplicate check, kernel push, or compute."
          echo "Use the argv-repaired successor workflow; this historical workflow must not be replayed."
          exit 1
