name: Retired CMI-Flu E06a 003 read-only recovery 001

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E06a 003 recovery
        shell: bash
        run: |
          echo "RETIRED: the user supplied the complete E06a 003 aggregate output archive, its hashes were validated, and the scientific result was recorded. No Kaggle recovery or replay is needed."
          exit 1
