name: Poisoned Chalice P1-03 post-5k CPU development audit v1

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/poisoned-chalice-p1-03-post-5k-development-audit-v1.yml
      - requests/poisoned-chalice-p1-03-post-5k-development-audit-v1.json
      - runners/poisoned_chalice/p1_03_post_5k_development_audit_v1.py
      - scripts/kaggle_exact_identity.py
      - requirements/kaggle-2.2.4.lock
  push:
    branches: [main]
    paths:
      - .github/workflows/poisoned-chalice-p1-03-post-5k-development-audit-v1.yml
      - requests/poisoned-chalice-p1-03-post-5k-development-audit-v1.json
      - runners/poisoned_chalice/p1_03_post_5k_development_audit_v1.py
      - scripts/kaggle_exact_identity.py
      - requirements/kaggle-2.2.4.lock

permissions: {}

concurrency:
  group: kaggle-api-global
  cancel-in-progress: false

env:
  EXPECTED_REPOSITORY: renta0426/kaggle-actions-bridge
  EXPECTED_REPOSITORY_ID: "1354356687"
  EXPECTED_ACTOR: renta0426
  EXPECTED_ACTOR_ID: "71638068"
  REQUEST_PATH: requests/poisoned-chalice-p1-03-post-5k-development-audit-v1.json
  LAUNCHER_PATH: runners/poisoned_chalice/p1_03_post_5k_development_audit_v1.py
  IDENTITY_PATH: scripts/kaggle_exact_identity.py
  LOCK_PATH: requirements/kaggle-2.2.4.lock
  TARGET: renta0426/poisoned-chalice-p1-03-post-5k-development-audit-v1
  STAGE1_DATASET: renta0426/stage1-raw-fim-submission-v1-output
  HIDDEN_CACHE: renta0426/poisoned-chalice-lumia-hidden-state-cache-5000-v1
  HIDDEN_EVAL: renta0426/poisoned-chalice-lumia-hidden-state-eval-5000-v1

jobs:
  validate:
    name: Validate frozen CPU request without credentials
    if: github.repository == 'renta0426/kaggle-actions-bridge' && github.actor == 'renta0426' && github.actor_id == '71638068'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Enforce secret-free validation boundary
        shell: bash
        env:
          EVENT_REPOSITORY: ${{ github.repository }}
          EVENT_REPOSITORY_ID: ${{ github.event.repository.id }}
          EVENT_ACTOR: ${{ github.actor }}
          EVENT_ACTOR_ID: ${{ github.actor_id }}
          EVENT_NAME: ${{ github.event_name }}
          EVENT_SHA: ${{ github.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "$EVENT_REPOSITORY" == "$EXPECTED_REPOSITORY" && "$EVENT_REPOSITORY_ID" == "$EXPECTED_REPOSITORY_ID" ]]
          [[ "$EVENT_ACTOR" == "$EXPECTED_ACTOR" && "$EVENT_ACTOR_ID" == "$EXPECTED_ACTOR_ID" ]]
          [[ "$RUNNER_ENVIRONMENT" == "github-hosted" ]]
          for name in KAGGLE_API_TOKEN KAGGLE_USERNAME KAGGLE_KEY GH_TOKEN GITHUB_PAT RESEARCH_REPO_READ_TOKEN SSH_PRIVATE_KEY; do
            [[ -z "${!name-}" ]] || { echo "unexpected credential variable: $name"; exit 1; }
          done
          source_sha="$EVENT_SHA"; if [[ "$EVENT_NAME" == "pull_request" ]]; then source_sha="$PR_HEAD_SHA"; fi
          [[ "$source_sha" =~ ^[0-9a-f]{40}$ ]]
          workdir="$(mktemp -d)"; echo "WORKDIR=$workdir" >> "$GITHUB_ENV"; echo "SOURCE_SHA=$source_sha" >> "$GITHUB_ENV"

      - name: Fetch exact bridge request and launcher
        shell: bash
        run: |
          set -euo pipefail
          python3 - "$SOURCE_SHA" "$WORKDIR" <<'PY'
          import pathlib,ssl,sys,urllib.request
          sha,root_value=sys.argv[1:]; root=pathlib.Path(root_value); ctx=ssl.create_default_context()
          files={
            'requests/poisoned-chalice-p1-03-post-5k-development-audit-v1.json':65536,
            'runners/poisoned_chalice/p1_03_post_5k_development_audit_v1.py':131072,
          }
          class NoRedirect(urllib.request.HTTPRedirectHandler):
            def redirect_request(self,req,fp,code,msg,headers,newurl): return None
          opener=urllib.request.build_opener(urllib.request.HTTPSHandler(context=ctx),NoRedirect())
          for path,maximum in files.items():
            url=f'https://raw.githubusercontent.com/renta0426/kaggle-actions-bridge/{sha}/{path}'
            req=urllib.request.Request(url,headers={'User-Agent':'p1-03-post5k-validate/1'})
            with opener.open(req,timeout=30) as response:data=response.read(maximum+1)
            if not data or len(data)>maximum:raise SystemExit(f'bridge source byte budget failed:{path}')
            out=root/pathlib.Path(path).name; out.write_bytes(data)
            if path.endswith('.py'):compile(data,path,'exec')
          PY

      - name: Validate science blobs and materialize exact Notebook
        shell: bash
        run: |
          set -euo pipefail
          python3 "$WORKDIR/p1_03_post_5k_development_audit_v1.py" --static --request "$WORKDIR/poisoned-chalice-p1-03-post-5k-development-audit-v1.json"
          python3 "$WORKDIR/p1_03_post_5k_development_audit_v1.py" --materialize --request "$WORKDIR/poisoned-chalice-p1-03-post-5k-development-audit-v1.json" --kernel-dir "$WORKDIR/kernel"
          python3 - "$WORKDIR/kernel/kernel-metadata.json" "$WORKDIR/poisoned-chalice-p1-03-post-5k-development-audit-v1.json" <<'PY'
          import json,sys
          meta=json.load(open(sys.argv[1])); req=json.load(open(sys.argv[2]))
          assert meta['id']==req['target']
          assert meta['is_private'] is True and meta['enable_gpu'] is False and meta['enable_tpu'] is False
          assert meta['enable_internet'] is True
          assert meta['dataset_sources']==['renta0426/stage1-raw-fim-submission-v1-output']
          assert meta['kernel_sources']==[
              'renta0426/poisoned-chalice-lumia-hidden-state-cache-5000-v1',
              'renta0426/poisoned-chalice-lumia-hidden-state-eval-5000-v1']
          assert req['automatic_compute_retries']==0 and req['competition_submission'] is False
          print('P1_03_POST_5K_BRIDGE_VALIDATE PASS cpu=1 write_calls=0 retries=0 submissions=0')
          PY

      - name: Cleanup validation material
        if: always()
        shell: bash
        run: |
          [[ -z "${WORKDIR-}" || ! -d "$WORKDIR" ]] || rm -rf "$WORKDIR"

  launch:
    name: Launch approved CPU development audit exactly once
    needs: validate
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && github.repository == 'renta0426/kaggle-actions-bridge' && github.actor == 'renta0426' && github.actor_id == '71638068'
    environment: kaggle-readonry
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Enforce protected execution boundary
        shell: bash
        env:
          EVENT_REPOSITORY: ${{ github.repository }}
          EVENT_REPOSITORY_ID: ${{ github.event.repository.id }}
          EVENT_ACTOR: ${{ github.actor }}
          EVENT_ACTOR_ID: ${{ github.actor_id }}
          EVENT_SENDER_ID: ${{ github.event.sender.id }}
          EVENT_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
          WORKFLOW_REF: ${{ github.workflow_ref }}
          RUNNER_ENVIRONMENT: ${{ runner.environment }}
        run: |
          set -euo pipefail
          [[ "$EVENT_REPOSITORY" == "$EXPECTED_REPOSITORY" && "$EVENT_REPOSITORY_ID" == "$EXPECTED_REPOSITORY_ID" ]]
          [[ "$EVENT_ACTOR" == "$EXPECTED_ACTOR" && "$EVENT_ACTOR_ID" == "$EXPECTED_ACTOR_ID" && "$EVENT_SENDER_ID" == "$EXPECTED_ACTOR_ID" ]]
          [[ "$EVENT_SHA" == "$WORKFLOW_SHA" ]]
          [[ "$WORKFLOW_REF" == "renta0426/kaggle-actions-bridge/.github/workflows/poisoned-chalice-p1-03-post-5k-development-audit-v1.yml@refs/heads/main" ]]
          [[ "$RUNNER_ENVIRONMENT" == "github-hosted" ]]
          echo "P1_03_POST_5K_PROTECTED_BOUNDARY PASS cpu=1 retries=0 submissions=0"

      - name: Fetch exact bridge bundle and install locked Kaggle client
        shell: bash
        env:
          EVENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          workdir="$(mktemp -d)"; echo "WORKDIR=$workdir" >> "$GITHUB_ENV"; echo "KERNEL_DIR=$workdir/kernel" >> "$GITHUB_ENV"
          python3 - "$EVENT_SHA" "$workdir" <<'PY'
          import pathlib,ssl,sys,urllib.request
          sha,root_value=sys.argv[1:]; root=pathlib.Path(root_value); ctx=ssl.create_default_context()
          files={
            'requests/poisoned-chalice-p1-03-post-5k-development-audit-v1.json':65536,
            'runners/poisoned_chalice/p1_03_post_5k_development_audit_v1.py':131072,
            'scripts/kaggle_exact_identity.py':65536,
            'requirements/kaggle-2.2.4.lock':65536,
          }
          class NoRedirect(urllib.request.HTTPRedirectHandler):
            def redirect_request(self,req,fp,code,msg,headers,newurl): return None
          opener=urllib.request.build_opener(urllib.request.HTTPSHandler(context=ctx),NoRedirect())
          for path,maximum in files.items():
            url=f'https://raw.githubusercontent.com/renta0426/kaggle-actions-bridge/{sha}/{path}'
            req=urllib.request.Request(url,headers={'User-Agent':'p1-03-post5k-launch/1'})
            with opener.open(req,timeout=30) as response:data=response.read(maximum+1)
            if not data or len(data)>maximum:raise SystemExit(f'bridge source byte budget failed:{path}')
            out=root/pathlib.Path(path).name; out.write_bytes(data)
            if path.endswith('.py'):compile(data,path,'exec')
          PY
          python3 -m venv "$workdir/venv"
          "$workdir/venv/bin/python" -m pip install --disable-pip-version-check --no-input --no-deps --only-binary=:all: --require-hashes -r "$workdir/kaggle-2.2.4.lock" >/dev/null
          "$workdir/venv/bin/python" -m pip check >/dev/null
          "$workdir/venv/bin/kaggle" --version | grep -F '2.2.4' >/dev/null
          "$workdir/venv/bin/python" "$workdir/p1_03_post_5k_development_audit_v1.py" --static --request "$workdir/poisoned-chalice-p1-03-post-5k-development-audit-v1.json"
          "$workdir/venv/bin/python" "$workdir/p1_03_post_5k_development_audit_v1.py" --materialize --request "$workdir/poisoned-chalice-p1-03-post-5k-development-audit-v1.json" --kernel-dir "$workdir/kernel"

      - name: Live exact-identity preflight and one-shot SaveKernel
        shell: bash
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          [[ -n "$KAGGLE_API_TOKEN" && "$KAGGLE_API_TOKEN" == KGAT_* ]]
          PYTHONPATH="$WORKDIR" "$WORKDIR/venv/bin/python" - "$WORKDIR/poisoned-chalice-p1-03-post-5k-development-audit-v1.json" <<'PY'
          import json,re,sys
          from kaggle.api.kaggle_api_extended import KaggleApi
          from kaggle_exact_identity import exact_metadata, safe_exception, verify_current
          req=json.load(open(sys.argv[1])); api=KaggleApi(); api.authenticate()

          pages=api.competition_list_pages(req['competition']) or []; content={}
          for page in pages:
              data=page.to_dict() if hasattr(page,'to_dict') else dict(page)
              name=str(data.get('name') or '').strip().lower()
              if name:content[name]=str(data.get('content') or '')
          if 'rules' not in content or 'evaluation' not in content or not any('data' in k for k in content):
              raise SystemExit('live competition contract pages unavailable')
          plain=re.sub(r'\s+',' ',re.sub(r'<[^>]+>',' ',content['evaluation'].replace(r'\%','%'))).casefold()
          if not all(token in plain for token in ('auc','novelty','1%','false-positive')):
              raise SystemExit('live evaluation contract changed')

          for source in req['inputs']['kernels']:
              state=verify_current(api, source['ref'], int(source['version']), allow_failed=False, cpu=False)
              if state!='COMPLETE':raise SystemExit('kernel input not complete')

          dataset=api.dataset_view(req['inputs']['dataset']['ref'])
          observed=getattr(dataset,'current_version_number',None)
          if observed is None:observed=getattr(dataset,'version_number',None)
          if observed is None or int(observed)!=int(req['inputs']['dataset']['version']):
              raise SystemExit(f"dataset current version mismatch: observed={observed!r}")

          try:
              exact_metadata(api,req['target'])
          except Exception as exc:
              info=safe_exception(exc)
              if info.get('http_status')!=404:raise SystemExit('target exact-absence check failed')
          else:
              raise SystemExit('approved target already exists; refusing overwrite')

          recent=(api.kernels_list(user='renta0426',sort_by='dateRun',page_size=25) or [])[:25]
          active=0; unknown=0
          for item in recent:
              ref=str(getattr(item,'ref','') or '')
              if not ref:continue
              try:status=str(getattr(api.kernels_status(ref),'status','')).upper()
              except Exception:unknown+=1;continue
              if any(token in status for token in ('RUNNING','QUEUED','PENDING')):active+=1
          if unknown:raise SystemExit(f'conservative admission refused: unknown_statuses={unknown}')
          if active>=int(req['resource']['max_active_runs']):
              raise SystemExit(f"concurrency admission refused: active={active} max_active={req['resource']['max_active_runs']}")
          print(f"P1_03_POST_5K_LIVE_PREFLIGHT PASS cpu=1 active={active} kernel_inputs=2 dataset_version=1 target_absent=1")
          PY
          "$WORKDIR/venv/bin/python" "$WORKDIR/p1_03_post_5k_development_audit_v1.py" --execute --request "$WORKDIR/poisoned-chalice-p1-03-post-5k-development-audit-v1.json" --kernel-dir "$KERNEL_DIR" --kaggle-bin "$WORKDIR/venv/bin/kaggle"

      - name: Verify exact target version after the single write
        shell: bash
        env:
          KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
        run: |
          set -euo pipefail
          PYTHONPATH="$WORKDIR" "$WORKDIR/venv/bin/python" - <<'PY'
          from kaggle.api.kaggle_api_extended import KaggleApi
          from kaggle_exact_identity import exact_metadata_eventually
          api=KaggleApi(); api.authenticate(); target='renta0426/poisoned-chalice-p1-03-post-5k-development-audit-v1'
          meta=exact_metadata_eventually(api,target,attempts=6,delay_seconds=2.0)
          if getattr(meta,'ref',None)!=target or getattr(meta,'is_private',None) is not True:raise SystemExit('post-write target identity mismatch')
          if int(getattr(meta,'current_version_number',0))!=1:raise SystemExit('post-write version is not exactly 1')
          if getattr(meta,'enable_gpu',None) is not False or getattr(meta,'enable_tpu',None) is not False:raise SystemExit('post-write accelerator contract mismatch')
          if getattr(meta,'enable_internet',None) is not True:raise SystemExit('post-write internet contract mismatch')
          print('P1_03_POST_5K_POSTWRITE_IDENTITY PASS target=exact version=1 cpu=1')
          PY

      - name: Cleanup runner-local material
        if: always()
        shell: bash
        run: |
          [[ -z "${WORKDIR-}" || ! -d "$WORKDIR" ]] || rm -rf "$WORKDIR"
          echo "P1_03_POST_5K_CLEANUP PASS retention_days=0"
