name: RETIRED CMI-Flu E05 current-version recovery 001

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired recovery
        run: |
          echo "RETIRED: E05 version-1 recovery completed and identified the locked-reference transport failure."
          exit 1
