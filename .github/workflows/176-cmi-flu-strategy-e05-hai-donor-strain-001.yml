name: RETIRED CMI-Flu E05 HAI donor strain CPU 001

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired one-shot launcher
        run: |
          echo "RETIRED: E05 001 was consumed and failed after exact write confirmation. Use the separately reviewed repaired request instead."
          exit 1
