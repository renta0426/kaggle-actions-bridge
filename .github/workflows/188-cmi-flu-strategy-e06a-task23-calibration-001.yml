name: Retired CMI-Flu strategy E06a Task2.3 calibration CPU 001

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E06a 001 replay
        shell: bash
        run: |
          echo "RETIRED: E06a 001 was written as version 1, exact identity was confirmed, and the remote Notebook failed on a zero fixed-panel-overlap D365 study. Replay is forbidden; use the repaired fresh-target E06a 002 request."
          exit 1
