name: Retired CMI-Flu E05 HAI donor strain CPU 006

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse retired E05 006 replay
        shell: bash
        run: |
          echo "RETIRED: E05 006 was written as version 1, exact identity was confirmed, and the remote run ended in ERROR. Replay is forbidden; use the fresh audited successor request."
          exit 1
