name: RETIRED CMI-Flu strategy E06b Task2.3 two-head CPU 001

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse replay of consumed E06b request
        shell: bash
        run: |
          echo "RETIRED: E06b 001 already created exact Kaggle version 1 and produced validated scientific outputs. Replay is forbidden."
          echo "The historical Actions failure occurred only because undeclared locked-reference staging files remained in saved output."
          exit 1
