name: Retired CMI-Flu E05 HAI donor strain CPU 002

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse consumed E05 002 launch
        run: |
          echo "RETIRED: E05 002 was consumed and failed with the diagnosed json_safe runtime binding defect."
          exit 1
