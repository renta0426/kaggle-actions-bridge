# Branch retention

`launch/shadow-gold-context-decomposition-v1` is a temporary implementation branch required by the current `main` ruleset. It must not be retained after its PR is merged. The scientific/runtime artifacts are retained on `main`; the branch has no independent archival role.
