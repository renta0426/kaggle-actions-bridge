# Poisoned Chalice Gold suffix-context decomposition v1

This bridge request launches the predeclared `shadow-gold-context-decomposition-v1` mechanistic experiment from research commit `7b289d67bfc56d886face8f5c1c89910f2177466`.

The preceding context-alignment run remains a formal selected-candidate FAIL, but its candidate envelope showed strong probe-boundary-free recovery. This follow-up does not select a replacement candidate. It freezes six mean-log-likelihood conditions to decompose suffix-region selection, the 256→254 crop, BOS/start-context alignment, and positional specificity.

Execution contract:

- exact public bridge snapshots only; protected job has no private-repository read credential;
- unchanged positive-control v2 training: 16 suffix-only exposures, 81,920 sequences, 1,280 optimizer steps per architecture;
- random-init GPT-2-style and Llama-style shadows on isolated Kaggle T4 x2;
- no candidate selection, logistic fit, weight search, probe text, or probe boundary;
- no Competition/external rows and no SmolLM2 labels;
- no Competition submission;
- automatic compute retries: 0;
- four persistent outputs only;
- Stage2-v3 and competition-feature promotion are forbidden by this run.

The PR validation path is credential-free and reconstructs/compiles the exact final Notebook. After merge, the resource-consuming launch remains separately gated by the protected `kaggle-readonry` Environment and repeats live Kaggle policy, Competition-page, GPU-quota, target-freshness, and concurrency admission checks before the single permitted `kernels push`.
