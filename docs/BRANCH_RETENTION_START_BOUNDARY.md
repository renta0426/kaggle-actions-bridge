# Temporary branch retention

`launch/shadow-gold-start-boundary-attribution-v1` exists only because bridge `main` requires pull requests. After the launch bundle is merged, this branch has no archival role and must be deleted. The unrelated CMI-Flu branch must not be touched.
